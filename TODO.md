# Northline – TODO (Short Term)

- [ ] Create Cargo workspace and empty crates (`core`, `crypto`, `desktop`).
- [ ] Add initial `northline-core` public types and traits (per ARCHITECTURE.md).
- [ ] Add stub modules for providers and tools.
- [ ] Add minimal Slint UI skeleton in `desktop`.
- [ ] Wire basic `run_query` call path (even if it returns a dummy answer).
