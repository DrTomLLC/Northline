pub mod errors;

pub use errors::{CoreError, ErrorCode};

// TODO: Add AppConfig, UserQuery, AnswerChunk, FinalAnswer, Source, ToolTrace,
// ModelBackend, Tool, and run_query per docs/ARCHITECTURE.md.
